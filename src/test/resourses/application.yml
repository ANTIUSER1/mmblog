server:
  port: 8899

spring:

  web:
    resources:
      static-locations: classpath:/static/,classpath:/public/,file:./webapps/
  mvc:
    static-path-pattern: /**


  application:
    name: mmblog

datasource:
  url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  username: sa
  password: password
  driverClassName: org.h2.Driver
#sql:
#  init:
#    mode: always
#    schema-locations: classpath:schema-test.sql
#    data-locations: classpath:data-test.sql
#  main:
#    # Это включит переопределение бинов
#    allow-bean-definition-overriding: true

